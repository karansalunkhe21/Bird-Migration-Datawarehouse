
name: 'syracuse_news_dwh'
version: '1.0.0'
config-version: 2

profile: 'syracuse_news_dwh'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  syracuse_news_dwh:
    staging:
      +materialized: view
      +schema: staging
    transform:
      +materialized: table
      +schema: transform
    marts:
      +materialized: table
      +schema: prod